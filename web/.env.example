# URL del worker SECOP API (sin barra final)
NEXT_PUBLIC_API_BASE=https://secop-api.example.workers.dev

# Muestra panel + logs en consola para respuestas GET (activo también en npm run dev)
# NEXT_PUBLIC_API_DEBUG=true
